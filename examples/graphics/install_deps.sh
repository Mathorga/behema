#!/bin/bash

# Install C++ compiler.
sudo apt install g++

# Install SFML graphics library.
sudo apt install libsfml-dev

# Install OpenCV for texture rendering.
sudo apt install libopencv-core-dev
sudo apt install libopencv-highgui-dev

printf "\nDependencies installed!\n"